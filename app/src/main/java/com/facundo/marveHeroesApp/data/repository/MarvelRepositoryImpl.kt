package com.facundo.marveHeroesApp.data.repository

import com.facundo.marveHeroesApp.data.model.heroes.HeroesDTO
//import com.facundo.marveHeroesApp.domain.repository.MarvelRepository
import com.superheroapp.data.network.ComicsApiClient
import javax.inject.Inject

/*
class MarvelRepositoryImpl @Inject constructor(
    private val api: ComicsApiClient
): MarvelRepository {
    override suspend fun getAllCharacters(offset: Int): HeroesDTO = api.getAllCharacters(offset = offset.toString())

}*/


